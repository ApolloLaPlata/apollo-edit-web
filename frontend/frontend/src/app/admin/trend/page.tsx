import React from 'react';
import TrendClient from './TrendClient';

export const metadata = {
  title: '🔮 Oráculo Preditivo | Apollo Auto-Blog CMS',
  description: 'Motor Preditivo de Tendências. Preveja e agende pautas virais antes mesmo delas estourarem, baseado em telemetria cross-channel.',
};

export default function TrendPage() {
  return <TrendClient />;
}
