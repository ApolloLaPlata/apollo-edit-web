import React from 'react';
import SeoHealerClient from './SeoHealerClient';

export const metadata = {
  title: '🛠️ Auto-Healer SEO | Apollo Auto-Blog CMS',
  description: 'Zelador Autônomo de SEO On-Page. Audita e cura metadados e Schema.org JSON-LD de matérias publicadas.',
};

export default function SeoHealerPage() {
  return <SeoHealerClient />;
}
