import React from 'react';
import SeoClient from './SeoClient';

export const metadata = {
  title: 'Auditoria SEO & Core Web Vitals • Auto-Blog CMS',
  description: 'Telemetria de performance em tempo real, validação de sitemaps e otimização neural de meta tags.'
};

export default function SeoPage() {
  return <SeoClient />;
}
