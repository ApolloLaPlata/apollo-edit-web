import React from 'react';
import CrossChannelClient from './CrossChannelClient';

export const metadata = {
  title: '📡 Telemetria Cross-Channel | Apollo Auto-Blog CMS',
  description: 'Dashboard executivo de tráfego orgânico consolidado em tempo real: YouTube, X, Instagram, TikTok, Telegram, Facebook, LinkedIn e Google Search.',
};

export default function CrossChannelPage() {
  return <CrossChannelClient />;
}
